# Sao chép file này thành .env rồi điền giá trị thực

# ── Database ──────────────────────────────────────────────────
DB_NAME=english_app
DB_USER=root
DB_PASSWORD=doi_mat_khau_nay      # <-- đổi thành mật khẩu mạnh
DB_PORT=3306

# ── Backend ───────────────────────────────────────────────────
PORT=5000
JWT_SECRET=doi_thanh_chuoi_ngau_nhien_dai_it_nhat_32_ky_tu
JWT_EXPIRES_IN=7d

# URL frontend truy cập vào (dùng cho CORS)
# Nếu chạy local: http://localhost
# Nếu có domain:  https://example.com
CLIENT_URL=http://localhost

# ── Email (tuỳ chọn) ──────────────────────────────────────────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your_email@gmail.com
MAIL_PASS=gmail_app_password
